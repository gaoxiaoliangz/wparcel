const help = () => {
  console.log(`--version, -v       show version
init                init project
serve               start webpack dev server
build               build assets
run                 run jellyweb tasks`)
}

module.exports = help
